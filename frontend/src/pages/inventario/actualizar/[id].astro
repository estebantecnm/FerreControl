---
export const prerender = false;
// Cambiamos el componente importado
import RestockProducto from '../../../components/vue/inventario/RestockProducto.vue';
import MainLayout from '../../../layouts/MainLayout.astro';

const { id } = Astro.params;
---

<MainLayout title="Entrada de Inventario">
    <div class="py-12 bg-slate-50 min-h-[calc(100vh-64px)]">
        <RestockProducto idProducto={id} client:only="vue" />
    </div>
</MainLayout>
